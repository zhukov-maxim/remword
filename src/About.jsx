import React from 'react';

var About = React.createClass({
  displayName: 'About',

  render: function () {
    return (<div className = 'about'>{'Temporary component for router debugging.'}</div>);
  }
});

export default About;
